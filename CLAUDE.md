# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Status

This project is empty — scaffolding only. As of 2026-07-28 it contains no source
files, no package manifest, no README, and is not a git repository. The CodeGraph
index (`.codegraph/`) is initialized but holds 0 files and 0 symbols.

There are consequently no build, test, or lint commands yet. **Replace this file
with real guidance once the codebase exists** — commands first, then the
cross-file architecture that isn't obvious from reading any single file.

## What's here

- `resume.sh` — starts a session via `claude --dangerously-skip-permissions`
- `.cursor/rules/codegraph.mdc` — CodeGraph MCP tool-selection guide; identical to
  the CodeGraph section already in the user's global `~/.claude/CLAUDE.md`, so it
  needs no restating here
