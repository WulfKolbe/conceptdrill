# Copy to .env and fill in. .env is gitignored and must never be committed.
# Read only from the environment; no code path loads a file outside this repo.
NOVITA_API_KEY=
NOVITA_API_BASE=https://api.novita.ai/v3/openai
NOVITA_MODEL=inclusionai/ling-3.0-flash
